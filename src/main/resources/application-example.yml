# Copy this file to application-dev.yml and fill in your values
# Never commit application-dev.yml to git!

spring:
  datasource:
    password: your-db-password

app:
  market:
    gold-api:
      api-key: your-gold-api-key